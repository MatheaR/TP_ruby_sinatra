development:
  adapter: postgresql
  host: localhost
  username: user
  database: app-dev
production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ<em>URL'] %>
      options:
        skip_version_check: true
        safe: true
